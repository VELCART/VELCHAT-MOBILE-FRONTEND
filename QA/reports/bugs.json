{
  "$comment": "Canonical VelChat QA bug registry. Single source of truth for the Excel report, the Jira sync and FINAL-QA-REPORT.md. 'layer' separates CLIENT (mobile repo) from BACKEND (D:\\Velchat) from INFRA (deployment) so triage goes to the right team. 'rootCause' is 'Unknown — requires engineering investigation' unless it was actually confirmed in code (§44).",
  "meta": {
    "project": "VelChat",
    "generatedBy": "QA automation suite",
    "run": "run-002",
    "environment": "local backend (axis6, gateway :8080) + Android emulator API 36",
    "build": "com.velchat.dev 1.0-dev (apk-out/velchat-20260909-2025.apk)",
    "reference_device_note": "The §M0 reference device is a 3 GB Android 10 handset. Testing here used an Android 16 (API 36) emulator, which is NOT the reference device — several defects (VC-006) are specifically Android <= 12 and could not be observed here."
  },
  "bugs": [
    {
      "id": "VC-001",
      "title": "Realtime fan-out silently drops up to 63% of durable messages sent within ~250ms of each other",
      "severity": "P0",
      "priority": "P0",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Realtime / Messaging",
      "component": "realtime fan-out (event bus -> ws-fabric)",
      "environment": "local backend axis6, gateway :8080, realtime :3001",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always (2/2 independent runs, identical numbers)",
      "reproducibility": "100% at 0-100ms send spacing; 0% at >=250ms spacing",
      "preconditions": "Two provisioned accounts A and B in one DM. Both hold an open WebSocket and have been greeted with `connected`.",
      "steps": [
        "Provision accounts A and B; create their DM.",
        "Open a WebSocket for each with a valid access token; wait for the `connected` frame.",
        "A sends 8 messages via POST /chat/messages back-to-back with no delay. Every send returns 201 with a distinct, monotonically increasing seq.",
        "Wait 5s for fan-out to settle.",
        "Count the durable `message` frames B's socket received.",
        "Repeat with 100ms, 250ms, 500ms, 1000ms and 1500ms spacing between sends."
      ],
      "expected": "All 8 messages arrive as durable `message` frames at B, at every spacing. The contract is explicit: 'Durable never dropped; ephemeral (typing/presence/receipt/caption) coalesced under backpressure' (docs/backend-integration-reference.md §4), and SendQueue's own docstring says 'durable frames (messages) are NEVER dropped' (libs/feature-realtime/src/fabric/send-queue.ts:10-13).",
      "actual": "Measured loss rate by spacing — 0ms: 3/8 delivered (63% lost); 100ms: 4/8 (50% lost); 250ms: 8/8 (0%); 500ms: 8/8; 1000ms: 8/8; 1500ms: 8/8. Every lost message WAS persisted (all 8 seqs are present in GET /chat/conversations/:id/messages?afterSeq=0), so this is purely a realtime delivery loss, not data loss.",
      "user_impact": "In a live open chat the recipient sees nothing for the majority of a rapid message burst. The sender's ticks never advance past one grey tick for those messages. The mobile client's cursor/afterSeq backfill only recovers them on a reconnect or a detected seq gap, so a user who stays in the foreground can sit in front of a chat that is visibly missing messages.",
      "suspected_cause": "The loss threshold sits at a clean ~250ms boundary, which points at a windowed read in the event-bus consumer between the durable publish and ws-fabric's write path: events arriving inside one window appear to overwrite rather than queue. The per-connection SendQueue was ruled OUT by reading it — durable frames are always pushed (send-queue.ts:22-31) and ws-fabric drains immediately on write (ws-fabric.ts:339-342), so nothing there can drop a durable frame.",
      "rootCause": "Unknown — requires engineering investigation.",
      "testIds": ["VC-RT-021", "VC-RT-020", "VC-RT-022"],
      "evidence": [
        "QA/evidence/websocket/VC-RT-021_FAIL_2026-09-11_fanout-loss-rate.log",
        "QA/evidence/websocket/VC-RT-021_repro.mjs",
        "QA/evidence/websocket/ws-2026-09-11.jsonl"
      ],
      "refs": [
        "docs/backend-integration-reference.md §4",
        "D:\\Velchat\\libs\\feature-realtime\\src\\fabric\\send-queue.ts:10-13",
        "D:\\Velchat\\libs\\feature-realtime\\src\\fabric\\ws-fabric.ts:339-342"
      ],
      "regression": false,
      "notes": "This one defect is the common cause behind VC-002, VC-003 and VC-004 — fix it first and re-test those before triaging them separately.",
      "jira": "VC-1"
    },
    {
      "id": "VC-002",
      "title": "Blue tick never arrives: a `read` receipt sent in the same tick as `delivered` is lost",
      "severity": "P0",
      "priority": "P0",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Receipts (blue tick)",
      "component": "receipt fan-out",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always when the two frames are sent without an intervening await (3/3 rounds)",
      "reproducibility": "100% same-tick; 0% when sent 1.5s apart",
      "preconditions": "A and B in a DM, both sockets open. A has sent a message acked with seq N.",
      "steps": [
        "A sends a message; note the acked seq N.",
        "B writes {kind:'durable',type:'delivered',data:{conversationId,seq:N}} and {kind:'durable',type:'read',data:{conversationId,seq:N}} to its socket with no delay between them.",
        "Wait 5s and list every `receipt` frame A received for seq N."
      ],
      "expected": "A receives both receipts, or at minimum the `read` one (read supersedes delivered), so the sender's tick turns blue.",
      "actual": "A receives only the `delivered` receipt — consistently the FIRST of the two. The `read` receipt never arrives, so the sender stays on a grey double tick indefinitely. Sending the same two frames 1.5s apart delivers both (verified: `delivered@2, read@2`).",
      "user_impact": "Blue ticks are effectively broken for the normal case. The mobile client marks a conversation read on open, which is exactly the same-tick pattern.",
      "suspected_cause": "Same windowing behaviour as VC-001, applied to receipt events. Note the surviving frame is the FIRST, not the latest — so it is not the documented ephemeral coalesce-to-latest in SendQueue (send-queue.ts:22-27), which would have kept `read`.",
      "rootCause": "Unknown — requires engineering investigation. Likely the same defect as VC-001.",
      "testIds": ["VC-RT-032", "VC-RT-031", "VC-RT-030"],
      "evidence": ["QA/evidence/websocket/ws-2026-09-11.jsonl", "QA/reports/results.jsonl"],
      "refs": [
        "apps/mobile/src/domain/sync/SyncEngine.ts:842-844",
        "docs/backend-integration-reference.md §5"
      ],
      "regression": false,
      "notes": "The client-side shape that triggers it: SyncEngine applies delivered then read for the same conversation with no gap.",
      "jira": "VC-2"
    },
    {
      "id": "VC-003",
      "title": "Read watermark can regress: the higher receipt is lost and a lower one survives",
      "severity": "P0",
      "priority": "P0",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Receipts (blue tick)",
      "component": "receipt fan-out",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Observed 1/1 in the out-of-order test",
      "reproducibility": "Tracks VC-001's loss window",
      "preconditions": "A has sent 3 messages to B (seqs 24, 25, 26). Both sockets open.",
      "steps": [
        "B sends a read receipt at the TOP seq (26).",
        "Wait 2s.",
        "B sends a read receipt at a LOWER seq (24).",
        "Wait 3s and record every read watermark A received."
      ],
      "expected": "The highest watermark A ever observes is 26. A read watermark must be monotonic — a blue tick must never turn back to grey.",
      "actual": "A received only [24]. The receipt for 26 was lost and the later, lower one survived, so A's highest observed watermark is 24 — lower than what B actually reported.",
      "user_impact": "Read state can move backwards on the sender's screen, or stall below the true watermark.",
      "suspected_cause": "Receipt loss (VC-001/VC-002) combined with the absence of a server-side monotonic floor on the fanned-out watermark. Note the CLIENT does enforce monotonicity in its query predicate, but non-atomically (see VC-021), so it cannot fully compensate.",
      "rootCause": "Unknown — requires engineering investigation.",
      "testIds": ["VC-RT-035"],
      "evidence": ["QA/reports/results.jsonl", "QA/evidence/websocket/ws-2026-09-11.jsonl"],
      "refs": [],
      "regression": false,
      "notes": "",
      "jira": "VC-3"
    },
    {
      "id": "VC-004",
      "title": "POST /auth/register accepts an arbitrary non-phone string and provisions a full account with real tokens",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Authentication / Registration",
      "component": "identity-service /auth/register",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "None. /auth/register is @Public.",
      "steps": [
        "POST /auth/register {phone:'not-a-phone', platform:'android', devicePubkeyBase64:<valid SPKI base64>}",
        "POST /auth/revotp/webhook with cli:'not-a-phone' and an otherwise-valid missed-call proof",
        "POST /auth/session {sessionId}"
      ],
      "expected": "Step 1 is rejected with a 4xx validation error. The phone number is the account's identity anchor — contact discovery and the OPRF blinding key are derived from it — so a non-phone string must never become an account.",
      "actual": "Step 1 returns 201. The webhook returns 200 {verified:true}. /auth/session returns 201 with a real accountId, deviceId, RS256 access JWT and opaque refresh token. A fully usable account exists whose phone is the literal string 'not-a-phone'.",
      "user_impact": "Unbounded junk-account creation, and an identity anchor that contact discovery cannot reason about. Any downstream code that assumes `phone` is E.164 now has a row that violates that.",
      "suspected_cause": "No phone validation on the register DTO. Note /auth/otp/send DOES validate (requireValidPhone in otp.service.ts), so the two entry points disagree.",
      "rootCause": "Unknown — requires engineering investigation (observed: no validation on this path; the OTP path validates).",
      "testIds": ["VC-AUTH-003", "VC-SEC-030"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": ["D:\\Velchat\\libs\\feature-auth\\src\\auth\\auth.service.ts:199-212"],
      "regression": false,
      "notes": "",
      "jira": "VC-11"
    },
    {
      "id": "VC-005",
      "title": "POST /auth/register accepts a non-base64 device public key, permanently breaking device-key login for that install",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Authentication / Device key",
      "component": "identity-service /auth/register",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "None.",
      "steps": [
        "POST /auth/register {phone:<valid>, platform:'android', devicePubkeyBase64:'!!!not-base64!!!'}",
        "Complete the Reverse-OTP proof.",
        "POST /auth/session."
      ],
      "expected": "Step 1 fails fast with a 4xx. The code comment at auth.service.ts:199 states exactly this intent: 'Fail-fast if the provided key is not valid base64. This prevents a 500 error later in the webhook.'",
      "actual": "All three steps succeed (201 / 200 / 201) and a real token set is issued. The account's device row now holds an unusable public key.",
      "user_impact": "The app's silent re-login path (apps/mobile/src/features/auth/hooks/useAuth.ts:286-291) calls /auth/challenge + /auth/login/device-key on cold start. For an install provisioned this way that can never verify, so the user is dropped to the sign-in screen on every cold start with no explanation.",
      "suspected_cause": "The guard the comment describes is either absent or not reached on this path.",
      "rootCause": "Unknown — requires engineering investigation.",
      "testIds": ["VC-AUTH-004", "VC-SEC-031"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": ["D:\\Velchat\\libs\\feature-auth\\src\\auth\\auth.service.ts:199"],
      "regression": false,
      "notes": "",
      "jira": "VC-12"
    },
    {
      "id": "VC-006",
      "title": "POST /auth/revotp/webhook is @Public with no shared secret — an unauthenticated account-provisioning primitive",
      "severity": "P0",
      "priority": "P0",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Security / Authentication",
      "component": "identity-service /auth/revotp/webhook",
      "environment": "local backend axis6; the same code is deployed to the dev Render environment",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "Network reach to the host. NO credentials of any kind.",
      "steps": [
        "POST /auth/register for any phone number (the endpoint is @Public) -> {sessionId}",
        "POST /auth/revotp/webhook with a self-asserted proof: {sessionId, cli:<same phone>, path:'missed-call', originationClass:'mobile', attestation:'genuine', riskScore:1, ts:Date.now()}",
        "POST /auth/session {sessionId} -> a real token set for that phone number."
      ],
      "expected": "The webhook must prove it came from the SIP gateway — a shared secret, a signature (the codebase already has `x-velchat-signature` for inter-service calls), or a network policy that makes the route unreachable from outside.",
      "actual": "The route is declared @Public (auth.controller.ts:31-36) and performs no origin check whatsoever. Every anti-spoof field it validates is supplied by the caller, so a caller simply asserts `originationClass:'mobile'`, `attestation:'genuine'`, `riskScore:1` and passes all five rules. Anyone who can reach the host mints tokens for an arbitrary phone number.",
      "user_impact": "Account takeover of any phone number that has not yet registered, and account creation at will. On a deployment reachable from the internet this is a complete authentication bypass.",
      "suspected_cause": "Acknowledged as incomplete in the code itself: 'Server-to-server only — in prod this edge is locked down by NetworkPolicy + a shared secret (platform task)' (auth.controller.ts:29-30). The platform task has not landed, and nothing fails closed in the meantime.",
      "rootCause": "Confirmed by code reading: the route has no authentication and no origin verification; the anti-spoof rules validate only caller-supplied fields.",
      "testIds": ["VC-SEC-032"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": [
        "D:\\Velchat\\libs\\feature-auth\\src\\auth\\auth.controller.ts:28-36",
        "D:\\Velchat\\libs\\feature-auth\\src\\auth\\reverse-otp\\reverse-otp.service.ts:82-112"
      ],
      "regression": false,
      "notes": "DISCLOSURE: the QA harness uses this endpoint to provision its test identities, because it is currently the only way to obtain two real sessions without spending SMS. QA/lib/provision.js refuses to run against any non-local host unless QA_ALLOW_REMOTE_PROVISION=true. When this defect is fixed, switch the harness to OTP dev-mode (OTP_DEV_MODE=true) as documented in RUNBOOK.md.",
      "jira": "VC-4"
    },
    {
      "id": "VC-007",
      "title": "Edge gateway 502s every proxied route while all upstream services are healthy (dev Render deployment)",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "INFRA",
      "feature": "Environment / Deployment",
      "component": "velchat-edge-gateway-2aje.onrender.com",
      "environment": "dev Render deployment",
      "device": "n/a",
      "os": "n/a",
      "frequency": "Always (throughout the test session)",
      "reproducibility": "100%",
      "preconditions": "All five axis6 services awake and returning 200 on /health at their own origins.",
      "steps": [
        "GET https://velchat-{identity,messaging,realtime,content,platform}-service-2aje.onrender.com/health -> 200 for all five.",
        "GET https://velchat-edge-gateway-2aje.onrender.com/health -> 200.",
        "GET https://velchat-edge-gateway-2aje.onrender.com/.well-known/jwks.json -> 502.",
        "Repeat for /auth/otp/send, /users/me, /presence/subscribe, /chat/messages, /notifications/prefs, /search, /media/usage -> 502 on every one."
      ],
      "expected": "The gateway proxies each path to its upstream and returns the upstream's response.",
      "actual": "The gateway's own /health and /ready return 200, but every proxied route returns 502 {error:{code:'BAD_GATEWAY'}} — including routes the upstream serves correctly when called directly (identity-service returns 200 for /.well-known/jwks.json and 401 for /auth/devices at its own origin).",
      "user_impact": "The mobile dev build points API_BASE_URL at this gateway (apps/mobile/.env.dev), so every API call fails. The dev app is completely unusable against the dev backend. This blocked all remote API testing and forced the suite onto a locally-started backend.",
      "suspected_cause": "Upstream host resolution in the gateway (the render.yaml wires upstream hosts via `fromService` host properties). Not investigated further — this is a deployment concern, not a code defect observable from the client.",
      "rootCause": "Unknown — requires engineering investigation.",
      "testIds": ["VC-ENV-001"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": ["apps/mobile/.env.dev", "D:\\Velchat\\render.yaml:84-122"],
      "regression": false,
      "notes": "Related client-side observation: warmBackend() (apps/mobile/src/infra/network/client.ts:33-62) pings the gateway, the gateway-proxied JWKS route and the realtime host. Because the gateway does not wake or reach its upstreams, that warm-up cannot actually warm identity/messaging/content/platform on Render. See VC-008.",
      "jira": "VC-13"
    },
    {
      "id": "VC-008",
      "title": "warmBackend() cannot warm the services it targets on the Render topology",
      "severity": "P2",
      "priority": "P2",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Networking / Cold start",
      "component": "apps/mobile/src/infra/network/client.ts:33-62",
      "environment": "dev Render deployment",
      "device": "n/a",
      "os": "n/a",
      "frequency": "Always on the Render topology",
      "reproducibility": "100%",
      "preconditions": "Render free-tier services hibernated after ~15 min idle.",
      "steps": [
        "Cold-start the app so warmBackend() fires.",
        "It requests <gateway>/health, <gateway>/.well-known/jwks.json and <realtime>/health.",
        "Observe which upstreams actually wake."
      ],
      "expected": "The login path (identity-service) is warm by the time the user taps 'Send code', which is the stated purpose of the function.",
      "actual": "Render wakes a service when a request reaches that service's OWN hostname. Only the gateway and the realtime host are addressed directly; identity/messaging/content/platform are only reached via the gateway, which returns 502 without waking them (VC-007). Measured: the gateway woke in 36s and realtime in 43s, while the four other services stayed asleep until each was hit at its own origin.",
      "user_impact": "The first real request after an idle period still eats a 30-50s cold start, which the 60s axios timeout barely covers — the exact failure warmBackend() was written to prevent.",
      "suspected_cause": "The warm-up list is built from the single gateway origin, which does not match the dev deployment's per-service Render topology.",
      "rootCause": "Confirmed by measurement: gateway-proxied requests do not wake upstream Render services.",
      "testIds": ["VC-ENV-002"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": ["apps/mobile/src/infra/network/client.ts:33-62"],
      "regression": false,
      "notes": "Blocked on VC-007: if the gateway reached its upstreams, a proxied ping would wake them and this would resolve itself.",
      "jira": "VC-14"
    },
    {
      "id": "VC-009",
      "title": "POST /conversations/dm returns 404 through the gateway — 'New chat' cannot work",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Conversations",
      "component": "edge-gateway route table / axis6 topology mapping",
      "environment": "local backend axis6, gateway :8080",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "A valid access token for account A; a second account B.",
      "steps": [
        "POST http://localhost:8080/conversations/dm {a:<A>, b:<B>} with A's bearer token.",
        "POST http://localhost:3002/conversations/dm with the same body and token (identity-service's own origin)."
      ],
      "expected": "Both succeed. The client only ever talks to the gateway, so the gateway route must work.",
      "actual": "Through the gateway: 404 {message:'Cannot POST /conversations/dm', path:'/conversations/dm'}. Direct to identity-service on :3002: 201 {conversationId:'dm-…', created:true}. The gateway route table sends /conversations/dm to the GROUP_CHANNEL logical service (routes.ts:32), which under the axis6 profile does not resolve to the service that mounts ChannelsController.",
      "user_impact": "Starting a new DM is impossible through the documented client base URL — the app's entire 'New chat' flow dead-ends.",
      "suspected_cause": "A topology mapping gap for GROUP_CHANNEL under SPLIT_PROFILE=axis6. The same class of defect is already documented in the repo for /status ('The combined presence|status rule sent these to realtime, where no StatusController exists, so the whole API 404'd under axis6/full13 and only worked under mono' — routes.ts:41-43).",
      "rootCause": "Unknown — requires engineering investigation.",
      "testIds": ["VC-API-020"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": [
        "D:\\Velchat\\apps\\edge-gateway\\src\\gateway\\routes.ts:32",
        "D:\\Velchat\\libs\\feature-group-channel\\src\\channels\\channels.controller.ts:38"
      ],
      "regression": false,
      "notes": "QA/lib/provision.js works around this by falling back to the identity-service origin, so downstream chat tests are not blocked. The workaround is documented in the file.",
      "jira": "VC-15"
    },
    {
      "id": "VC-010",
      "title": "Sign-out's authenticated calls ship with no Authorization header, so the server-side push endpoint is never cleared",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Authentication / Push",
      "component": "apps/mobile/src/features/auth/model/authStore.ts:79-89",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic by code path",
      "preconditions": "A signed-in session with a registered push endpoint.",
      "steps": [
        "Sign out.",
        "Observe the outbound POST /auth/logout and POST /notifications/endpoints (clear) requests."
      ],
      "expected": "Both carry `Authorization: Bearer <access>` — the code comments at authStore.ts:83-88, pushService.ts:521-522, infra/push/api.ts:34-36 and pushRuntime.ts:162-163 all state they are sent 'while the token is still valid'.",
      "actual": "clearSession() (authStore.ts:89) is synchronous, while axios 1.18.1 runs a non-`{synchronous:true}` request interceptor in a microtask. So the interceptor at client.ts:226-246 reads `auth.accessToken` AFTER clearSession() has deleted it, and both fire-and-forget calls reach the server unauthenticated. The server-side push endpoint row is never cleared.",
      "user_impact": "A signed-out device keeps receiving push wake-ups for the account that signed out, and the refresh family is not revoked server-side.",
      "suspected_cause": "Microtask ordering between a synchronous storage clear and an async axios interceptor.",
      "rootCause": "Confirmed by code reading (axios Axios.js:161-189 gates synchronous interceptor execution on the `synchronous` flag, which is not set).",
      "testIds": ["VC-UNIT-101"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d1"],
      "refs": [
        "apps/mobile/src/features/auth/model/authStore.ts:79-89",
        "apps/mobile/src/infra/network/client.ts:226-246"
      ],
      "regression": false,
      "notes": "FIX: The request interceptor is now registered with {synchronous:true} (client.ts), so axios invokes it INLINE at the moment a request is issued instead of deferring it into a microtask. A call fired immediately before a synchronous clearSession() now reads the still-current token, closing the race. Verified: full jest suite 454/454 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-16",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-011",
      "title": "Sign-out then sign-in in one process silently destroys every notification action",
      "severity": "P0",
      "priority": "P0",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Push / Notifications",
      "component": "apps/mobile/src/app/App.tsx:133-136 + features/notifications/model/pushRuntime.ts:148-159",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always after a sign-out/sign-in without killing the app",
      "reproducibility": "Deterministic by code path",
      "preconditions": "The app has been signed in, then signed out, then signed in again — all without a process restart.",
      "steps": [
        "Sign in, confirm push works (reply from a notification succeeds).",
        "Sign out (this calls shutdownPushForSignOut -> stopPushRuntime).",
        "Sign in again with the same or another number.",
        "Receive a message and reply from the notification."
      ],
      "expected": "The reply is sent, as it was before the sign-out.",
      "actual": "startPushRuntime() runs once with `[]` deps and is never called again. stopPushRuntime() removed both the push-event listener and `unsubSession` — the only thing that re-runs initPush() after a login. Meanwhile disposePush() is never called in app code (only from __resetPushForTests), so pushService's own AppState listener survives and keeps calling drainPendingEvents() on every foreground. That drains the native queue (PushStore.kt:403-407 deletes it) into an EMPTY eventListeners set. Every reply / mark-read / mute pressed after the re-login is drained and thrown away, and the new account never registers a push endpoint.",
      "user_impact": "Notification replies are silently lost after a re-login, and the new account gets no push at all. The user sees their reply disappear with no error.",
      "suspected_cause": "Asymmetric lifecycle: the runtime can be stopped but never restarted, and two separate dispose paths (stopPushRuntime vs disposePush) with only one of them wired.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-102"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d2"],
      "refs": [
        "apps/mobile/src/features/notifications/model/pushRuntime.ts:148-159",
        "apps/mobile/src/infra/push/pushService.ts:224-234,338-348,649"
      ],
      "regression": false,
      "notes": "FIX: provision() now calls startPushRuntime() too (idempotent — harmless no-op on the normal first sign-in the App.tsx mount effect already started). A re-login after shutdownPushForSignOut() (sign-out) in the same process now correctly re-establishes the availability/message/session subscriptions instead of leaving them permanently torn down. Verified: full jest suite 455/455 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-5",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-012",
      "title": "Android <= 12 reports notifications as granted even when disabled, so the sync engine drops its background socket",
      "severity": "P0",
      "priority": "P0",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Push / Notifications",
      "component": "apps/mobile/src/infra/native/notifications.ts:21-23",
      "environment": "any",
      "device": "3 GB Android 10 (the §M0 reference device)",
      "os": "Android <= 12 (API < 33)",
      "frequency": "Always on API < 33 with notifications disabled",
      "reproducibility": "Deterministic by code path — NOT observable on the API 36 emulator used for this run",
      "preconditions": "An Android 10-12 device with VelChat notifications turned off in system settings.",
      "steps": [
        "Turn off notifications for VelChat in Android system settings.",
        "Launch the app and sign in.",
        "Background the app.",
        "Have another user send a message."
      ],
      "expected": "The app detects that notifications are blocked, keeps its background socket (since push cannot wake it), and surfaces the push blocker banner.",
      "actual": "hasNotificationPermission() returns a hardcoded `true` below API 33. That feeds refreshPermission() (pushService.ts:397-399) -> isPushAvailable() true (pushState.ts:120-123) -> pushRuntime.ts:116-118 calls syncEngine.setPushAvailable(true), and the engine drops its background socket. Notifications are blocked so nothing displays either: total silence. The correct app-level check already exists natively (areNotificationsEnabled(), PushNotifications.kt:173) and is used by the banner but not by the state that gates the socket.",
      "user_impact": "On the stated reference device, a user who has notifications off receives nothing at all in the background — no notification and no socket. Messages appear only when the app is reopened.",
      "suspected_cause": "The pre-API-33 branch conflates 'no runtime permission is required' with 'notifications are enabled'.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-103"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d6"],
      "refs": [
        "apps/mobile/src/infra/native/notifications.ts:21-23",
        "apps/mobile/android/app/src/main/java/com/velchat/push/PushNotifications.kt:173"
      ],
      "regression": false,
      "notes": "FIX: refreshPermission() (pushService.ts) trusted hasNotificationPermission() alone, which on Android <33 has no runtime dialog and unconditionally answers true regardless of the app-level or channel-level notification toggle the user can flip from system Settings. Combined it with the same native areMessageNotificationsBlocked() check the push blocker banner already used (getPushBlocker()), via a new pure decision function notificationsGranted(permitted, blocked) in pushState.ts, so permission now genuinely means the OS will show something on every API level before isPushAvailable lets the SyncEngine drop its background socket. No native code changed — reused the existing native primitive. Verified: full jest suite 456/456 pass (452 + 4 new), tsc --noEmit clean, eslint 0 errors on touched files. Could not be observed on a real Android <=12 device (none available on this machine) — the fix is verified at the unit-test level against the exact documented defect (permitted=true, blocked=true -> denied), not end-to-end on hardware.",
      "jira": "VC-6",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-013",
      "title": "clearSession() is not a session teardown — the previous account's chat DB, profile and contacts survive into the next sign-in",
      "severity": "P0",
      "priority": "P0",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Authentication / Data isolation",
      "component": "apps/mobile/src/infra/network/tokens.ts:179-187",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always on the forced-logout path",
      "reproducibility": "Deterministic by code path",
      "preconditions": "An active session that the server then rejects (refresh-token family revoked, e.g. after the replay in VC-014).",
      "steps": [
        "Sign in as account A and exchange messages so the local DB, profile mirror and contact cache are populated.",
        "Cause the server to reject the refresh token (a family revoke), so client.ts:289 calls clearSession().",
        "Sign in with a DIFFERENT phone number."
      ],
      "expected": "The forced-logout path performs the same teardown as signOut(): purge chat data, profile mirror, receipts, contact discovery cache, conversation-peer cache and native push state.",
      "actual": "clearSession() drops five KV keys and nothing else; authStore.sessionExpired() (authStore.ts:131-133) only flips the state. NONE of the ~15 purges signOut() performs (authStore.ts:88-123) run — no shutdownPushForSignOut, no purgeAllLocalChat, no profile-mirror delete, no KVKeys.phone delete, no clearAllReceipts/clearProfileCache/clearContactsDiscoveryCache/clearConversationPeerCache/clearStartDmCache, no native PushStore.clearSession(). The new account inherits all of it, while App.tsx:91-93 backfills the new account's inbox on top.",
      "user_impact": "Account B sees account A's chats, name, phone number, contacts and receipts. This is a cross-account data-leak on a shared or resold device.",
      "suspected_cause": "Two logout paths with different responsibilities; only the user-initiated one performs the purge.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-104"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d10"],
      "refs": [
        "apps/mobile/src/infra/network/tokens.ts:179-187",
        "apps/mobile/src/features/auth/model/authStore.ts:88-123"
      ],
      "regression": false,
      "notes": "FIX: sessionExpired() now runs the same purgeAccountData() as signOut() (extracted, shared), minus clearDeviceKey() (a transient expiry must still allow a later silent relogin) and the network logout call (pointless — the session is already rejected). The next sign-in on this device, even under a different phone number, no longer inherits the previous account chat DB, profile, contacts or receipts. Verified: full jest suite 455/455 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-7",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-014",
      "title": "Rotating-refresh replay is structurally inevitable on a lost response, and the family revoke is then silently swallowed",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Authentication / Tokens",
      "component": "apps/mobile/src/infra/network/client.ts:160-197",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever a refresh response is lost (Render cold start, flaky network)",
      "reproducibility": "Deterministic given a lost response",
      "preconditions": "A signed-in session; a refresh request whose response never arrives.",
      "steps": [
        "Trigger a token refresh and drop the response (timeout / network loss).",
        "doRefresh returns {status:'unavailable'} and KEEPS the refresh token the server has already rotated.",
        "Trigger another 401 so the same refresh token is submitted again.",
        "Cold-start the app."
      ],
      "expected": "The client recognises that a refresh may have succeeded server-side and does not replay the same token; a family revoke forces a visible re-login.",
      "actual": "The rotated token is resubmitted, the server detects reuse and revokes the whole family (confirmed server-side by test VC-AUTH-024). The user is force-logged-out with no explanation. Because clearDeviceKey() only runs in signOut(), the next cold launch silently re-logs in via loginWithDeviceKey (useAuth.ts:286-291) — so the security signal is discarded rather than surfaced.",
      "user_impact": "Unexplained logouts on a flaky network, and a revoked-family security event that never reaches the user or telemetry.",
      "suspected_cause": "No 'this refresh token was already submitted' marker in tokens.ts.",
      "rootCause": "Confirmed by code reading, and the server-side family revoke is confirmed by live test VC-AUTH-024.",
      "testIds": ["authStore.test.ts: VC-014"],
      "evidence": ["QA/reports/results.jsonl", "QA/_audit/auth-network-push-audit.md#d3"],
      "refs": [
        "apps/mobile/src/infra/network/client.ts:160-197",
        "docs/backend-integration-reference.md:41"
      ],
      "regression": false,
      "notes": "FIX (investigated against the real backend source at D:Velchat, read-only): confirmed a refresh-reuse family revoke and a plain invalid/expired refresh both return an identical 401 UNAUTHORIZED with no distinguishing code, and confirmed device-key login is architecturally INDEPENDENT of the refresh-token family (revokeFamily only touches refresh_tokens; the devices table has no family_id) - so a device that still holds its private key can always mint a fresh session, and the client cannot and need not distinguish reuse-triggered revokes from ordinary rejections. Two real defects fixed: (1) clearSession() was deleting KVKeys.deviceId, which meant NEITHER the existing cold-start silent-relogin (useAuthBootstrap) NOR any warm recovery could ever actually reach /auth/challenge after a forced expiry, despite the code comments claiming this already worked - moved the explicit deviceId wipe into signOut() only, so a real logout is unchanged but a forced expiry now keeps it, same as the device key. (2) added attemptSilentRelogin() (authStore.ts), invoked from sessionExpired() itself, so a lost-refresh-response false-positive family revoke recovers IMMEDIATELY while the app is warm instead of only on the next cold start, turning a network hiccup back into nothing instead of stranding the user on the sign-in screen. Did not attempt to prevent the reuse-detection race itself (structurally requires a backend idempotency mechanism - out of scope, frontend-only per current instruction). Verified: full jest suite 481/481 pass, tsc --noEmit clean, eslint 0 errors. No RN-level test existed before; added 4 new tests (authStore.test.ts) covering the success, no-device-key, device-revoked and sessionExpired-wiring paths - the second and third caught the deviceId-wipe bug during RED before the fix was written.",
      "jira": "VC-17",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-015",
      "title": "The cnfJkt DPoP binding is dead code — the refresh token is bound to nothing",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Security / Tokens",
      "component": "apps/mobile/src/infra/network/client.ts:167,184-185 + tokens.ts:172",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic by code path",
      "preconditions": "Any refresh.",
      "steps": [
        "Sign in.",
        "Trigger a token refresh.",
        "Inspect the outbound POST /auth/token/refresh body."
      ],
      "expected": "`cnfJkt` carries the device key's JWK thumbprint, binding the refresh token to this device — the one piece of DPoP this backend implements.",
      "actual": "KVKeys.cnfJkt is only written by setTokens when t.cnfJkt !== undefined, and its only callers are authStore.provision (which never sets it) and doRefresh (which reads the same empty key back into itself). Every refresh sends cnfJkt: undefined. No thumbprint is computed anywhere — deviceKey.ts has no JWK/thumbprint function.",
      "user_impact": "A stolen refresh token is redeemable from any device.",
      "suspected_cause": "The binding was scaffolded but the thumbprint computation was never implemented.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-106"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d4"],
      "refs": [
        "apps/mobile/src/infra/network/tokens.ts:25-27,172",
        "docs/backend-integration-reference.md §3"
      ],
      "regression": false,
      "notes": "FIX: Added cnfJktThumbprint() (deviceKey.ts) — a real RFC 7638 JWK thumbprint (RFC 8037 OKP encoding) over the device Ed25519 public key. doRefresh() now computes it fresh on every refresh instead of round-tripping through an MMKV key nothing ever wrote a real value into. Verified: full jest suite 454/454 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-18",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-016",
      "title": "MMKV's encryption key is a hardcoded bundle constant, and the Ed25519 device private key is stored inside it",
      "severity": "P0",
      "priority": "P0",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Security / Key storage",
      "component": "apps/mobile/src/infra/kv/mmkv.ts:10-16",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic",
      "preconditions": "Read access to the APK, or filesystem access to the app's data directory.",
      "steps": [
        "Extract the APK and read the string constant at infra/kv/mmkv.ts:10-16.",
        "Use it to decrypt the MMKV store.",
        "Read auth.accessToken, auth.refreshToken, auth.phone and auth.devicePrivKey."
      ],
      "expected": "Per §L14, the device key is a non-exportable Secure Enclave / StrongBox key that only exposes sign(); tokens are protected by a key derived from the platform keystore.",
      "actual": "ENCRYPTION_KEY is the literal string 'velchat-mp0-placeholder-key-derive-from-keychain-in-mp1', compiled into every build. The store holds the access token, refresh token, cnfJkt, phone number and — via deviceKey.ts:44 — the raw Ed25519 private key as hex. That private key is exactly what POST /auth/login/device-key accepts as proof of possession.",
      "user_impact": "Anyone who can read the app's data directory obtains a permanent credential for the account: the device identity itself, which grants tokens on demand and survives a password or session reset.",
      "suspected_cause": "Deliberate MP0 placeholder — both files carry honest TODO(MP1) notes.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-SEC-101"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#s1"],
      "refs": [
        "apps/mobile/src/infra/kv/mmkv.ts:10-16",
        "apps/mobile/src/infra/crypto/deviceKey.ts:44"
      ],
      "regression": false,
      "notes": "Known and tracked as MP1 work, but it is the single highest-severity item in the client and should gate any release.",
      "jira": "VC-8"
    },
    {
      "id": "VC-017",
      "title": "Message plaintext and the user's own typed replies persist in plaintext SharedPreferences",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Security / Notifications",
      "component": "android/app/src/main/java/com/velchat/push/PushStore.kt:28-31,248-273,394-400",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic",
      "preconditions": "Notifications received; a reply typed from a notification.",
      "steps": [
        "Receive several messages so notification lines accumulate.",
        "Reply from a notification.",
        "Read the app's SharedPreferences file."
      ],
      "expected": "No personal message content at rest in plaintext; §M0 forbids plaintext of personal (E2EE) content in the DB or logs.",
      "actual": "KEY_LINES stores up to 6 lines x 20 conversations of message bodies; KEY_PENDING stores up to 64 queued events including reply text. EncryptedSharedPreferences was deliberately declined, with a header comment justifying it for the FCM token — which does not address personal content. showOwnReply (PushNotifications.kt:451-455) writes the user's freshly typed message, which the E2EE path would never otherwise put on disk unencrypted. cancelAll (PushNotifications.kt:533-536) clears counts but not KEY_LINES, so the content outlives the visible notification.",
      "user_impact": "Message content readable from device storage after the notification is dismissed.",
      "suspected_cause": "The storage decision was made for the token and extended to content without re-evaluation.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-SEC-102"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#s4"],
      "refs": ["android/app/src/main/java/com/velchat/push/PushStore.kt:28-31"],
      "regression": false,
      "notes": "Needs an ADR or, at minimum, a scrub-on-cancel.",
      "jira": "VC-19"
    },
    {
      "id": "VC-018",
      "title": "No certificate pinning anywhere, in JS or native",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Security / Transport",
      "component": "repo-wide",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic",
      "preconditions": "A compromised or rogue system CA.",
      "steps": [
        "Search the repo for pinning configuration.",
        "Check android/app/src/main/res/xml/ for a networkSecurityConfig."
      ],
      "expected": "§A1/§D4 require certificate pinning with a stapled fallback.",
      "actual": "No pinning, no TrustKit; android/app/src/main/res/xml/ does not exist. The native ack path (PushAckClient.kt:196-244, raw HttpURLConnection) has no pinning either and would need it separately.",
      "user_impact": "A rogue system CA can read tokens, OTPs and phone numbers. Apps targeting API >= 24 already exclude user-added CAs, so a casual MITM proxy is blocked — the exposure is a compromised system CA.",
      "suspected_cause": "Not yet implemented.",
      "rootCause": "Confirmed by exhaustive search.",
      "testIds": ["VC-SEC-103"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#s2"],
      "refs": [],
      "regression": false,
      "notes": "Grouped with VC-019: four of the five §A1/§D4 controls have no implementation.",
      "jira": "VC-20"
    },
    {
      "id": "VC-019",
      "title": "Threat-model controls absent: no FLAG_SECURE, no Play Integrity, no clipboard auto-clear, no root signal",
      "severity": "P2",
      "priority": "P2",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Security / Threat model",
      "component": "repo-wide (§A1/§D4)",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic",
      "preconditions": "None.",
      "steps": [
        "Search for FLAG_SECURE, Play Integrity / App Attest, clipboard clearing and root detection."
      ],
      "expected": "The §A1/§D4 controls are present, especially screenshot protection on the phone-entry and OTP screens.",
      "actual": "None of the four are implemented.",
      "user_impact": "OTP and phone-entry screens are screenshottable and appear in the recents thumbnail; enrollment has no device-integrity signal.",
      "suspected_cause": "Not yet implemented.",
      "rootCause": "Confirmed by exhaustive search.",
      "testIds": ["VC-SEC-104"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#s8"],
      "refs": [],
      "regression": false,
      "notes": "Stated as coverage, not as a regression.",
      "jira": "VC-21"
    },
    {
      "id": "VC-020",
      "title": "Concurrent applyServerMessages duplicates every row in the overlap, and can lose a whole batch on a conversation PK clash",
      "severity": "P0",
      "priority": "P0",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Offline sync / Local DB",
      "component": "apps/mobile/src/infra/db/messages.ts:202-234",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever two applies overlap on one conversation",
      "reproducibility": "Deterministic given the interleaving",
      "preconditions": "A reconnect that starts resyncAll() while a gapped live frame triggers a gap probe on the same conversation.",
      "steps": [
        "Force a reconnect so resyncAll() begins (SyncEngine.ts:677, no single-flight guard).",
        "Deliver a live message frame with a seq gap on a conversation resyncAll is already applying, so the gap probe calls backfillConversation() (SyncEngine.ts:751).",
        "Inspect the messages table for the overlapping range."
      ],
      "expected": "One row per message. The codebase documents this exact hazard at SyncEngine.ts:192-198 and guards loadOlderMessages with `loadingOlder`.",
      "actual": "The dedup snapshot is read OUTSIDE db.write (messages.ts:202-232 vs :234). WatermelonDB serialises writers, so two overlapping applies each conclude the same rows are new and both insert them. For a new DM both also prepareCreate the same conversation PK (messages.ts:311); the second batch is rejected and ALL of its messages are lost, logged only as 'resync conversation failed' (SyncEngine.ts:1009).",
      "user_impact": "Duplicate message bubbles after a reconnect, or a silently missing block of messages in a newly created DM.",
      "suspected_cause": "Read-modify-write across the transaction boundary; the guard that exists for loadOlderMessages was not applied to resyncAll, backfillConversation or the content-fill path.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-201"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": [
        "apps/mobile/src/infra/db/messages.ts:202-234",
        "apps/mobile/src/domain/sync/SyncEngine.ts:192-198,677,751,963"
      ],
      "regression": false,
      "notes": "VC-001 makes this far more likely to be hit in practice, because seq gaps are constant. FIX: The three lookup queries in applyServerMessages moved inside db.write(), read before any prepareCreate/prepareUpdate call — closes the race without violating the prepare-then-batch-synchronously constraint. Verified: full jest suite 454/454 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-9",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-021",
      "title": "applyReceipt is a non-atomic read-modify-write, so blue ticks can regress to grey",
      "severity": "P0",
      "priority": "P0",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Receipts",
      "component": "apps/mobile/src/infra/db/messages.ts:480-491",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever two receipt applications race",
      "reproducibility": "Deterministic given the interleaving; invisible when applied sequentially",
      "preconditions": "Two receipt applications in flight for the same conversation.",
      "steps": [
        "Arrange for a `read` receipt and a late `delivered` receipt to be applied concurrently (SyncEngine.ts:485, :842 and useMessages.ts:79 all call applyReceipt without awaiting each other).",
        "Observe the stored message state."
      ],
      "expected": "Receipt state is monotonic: delivered -> read, never backwards.",
      "actual": "Monotonicity lives ONLY in the query predicate Q.where('state', Q.oneOf(behind)), evaluated at fetch time (messages.ts:480-489) — outside the db.write at :491. Three independent un-awaited callers race, so a late `delivered` can be applied on top of a `read`.",
      "user_impact": "A blue double tick turns back to grey.",
      "suspected_cause": "The monotonic guard is in the read, not in the write.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-202"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": ["apps/mobile/src/infra/db/messages.ts:480-491"],
      "regression": false,
      "notes": "Every existing receipt test applies receipts sequentially, which is exactly why they all pass. FIX: applyReceipt now re-fetches the rows to update from inside db.write() (same pattern as VC-020) instead of before it, so the monotonic check is atomic with the write. Verified: full jest suite 454/454 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-10",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-022",
      "title": "A SendAck with seq 0 leaves the message permanently un-tickable and permanently duplicated",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Messaging / Send",
      "component": "apps/mobile/src/infra/network/chat.ts:140 -> infra/db/messages.ts:373",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever an ack omits or zeroes seq",
      "reproducibility": "Deterministic given such an ack",
      "preconditions": "A SendAck whose seq field is missing or 0.",
      "steps": [
        "Send a message and have the ack arrive without a usable seq (normalizeSendAck applies `pickNum(d,'seq') ?? 0`).",
        "Observe the local row and the tick state over time."
      ],
      "expected": "An ack without a seq is treated as a failure, or the row is repaired by the cursor backfill.",
      "actual": "seq 0 is written unconditionally (messages.ts:373) and the state flips to `sent`, so it looks fine. But the `seq > 0` filters at messages.ts:444 and :485 make the row invisible to the cursor and it can never be ticked again; the dup-destroy at :356 will not collapse the WS echo, leaving a permanent duplicate bubble.",
      "user_impact": "A message stuck at one grey tick forever, shown twice.",
      "suspected_cause": "A defaulting `?? 0` where the absence of a seq should be an error.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-203"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": [
        "apps/mobile/src/infra/network/chat.ts:140",
        "apps/mobile/src/infra/db/messages.ts:356,373,444,485"
      ],
      "regression": false,
      "notes": "normalizeSendAck has no test file at all. FIX: The outbox drain now rejects a SendAck with no usable seq (ack.seq is not > 0) as a retryable server-kind AppError instead of writing it as a successful send — routes through the exact same retry path as any other transient send failure, so the row never gets stuck permanently un-tickable/duplicated. Verified: full jest suite 455/455 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-22",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-023",
      "title": "One 401 on send turns the entire outbox into permanently failed messages",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Messaging / Outbox",
      "component": "apps/mobile/src/infra/db/sendFailurePolicy.ts:59-61 + domain/sync/SyncEngine.ts:1155-1161",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever a send hits 401/403 with a queue behind it",
      "reproducibility": "Deterministic",
      "preconditions": "Several queued messages and an access token the server rejects.",
      "steps": [
        "Queue 5 messages offline.",
        "Come back online with a token the server 401s.",
        "Observe every queued message."
      ],
      "expected": "A 401 pauses the drain, refreshes, and retries — a transient auth problem must not destroy queued user content.",
      "actual": "The policy maps `auth` to {permanent:true, pauseDrain:false}, so the walk continues and every queued item hits the same 401 and is marked permanently failed. Every message becomes a red bubble. Note client.ts:286-288 explicitly says a refresh that could not reach the server leaves the session valid and should surface a RETRYABLE error — the outbox policy does the opposite.",
      "user_impact": "A whole queue of unsent messages is destroyed by one transient auth failure — the classic overnight-background shape.",
      "suspected_cause": "Policy choice, not an accident: sendFailurePolicy.test.ts:52 asserts this behaviour.",
      "rootCause": "Confirmed by code reading. This is a deliberate policy that should be revisited, not a regression.",
      "testIds": ["VC-UNIT-204"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": [
        "apps/mobile/src/infra/db/sendFailurePolicy.ts:59-61",
        "apps/mobile/src/infra/db/__tests__/sendFailurePolicy.test.ts:52"
      ],
      "regression": false,
      "notes": "FIX: classifySendFailure() grouped auth with client, treating a post-refresh-failure 401/403 as if THIS message were rejected (permanent:true, pauseDrain:false) — one transient auth hiccup then permanently failed every other queued message as the drain kept walking into the same 401. auth is a SESSION problem, not a message problem (client.ts already treats a refresh that cannot reach the server as retryable, leaving the session valid). Added a dedicated auth case returning {permanent:false, pauseDrain:true, cooldownMs:0}, matching the network/timeout/server treatment, so the drain pauses instead of destroying the queue and resumes once the session recovers. Split the existing it.each(['client','auth']) test (which encoded the old, wrong grouping) into client-only plus a new dedicated describe block for auth. Verified: full jest suite 452/452 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-23",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-024",
      "title": "Migration-v3's five composite indexes never exist on a fresh install",
      "severity": "P2",
      "priority": "P2",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Local DB / Performance",
      "component": "apps/mobile/src/infra/db/migrations.ts:46-77",
      "environment": "any",
      "device": "3 GB Android 10 (worst-device-first, §M0)",
      "os": "any",
      "frequency": "Always on a fresh install",
      "reproducibility": "Deterministic",
      "preconditions": "A brand-new install (no prior schema version).",
      "steps": [
        "Install fresh.",
        "Inspect the SQLite schema for messages_window_idx and the other four composite indexes."
      ],
      "expected": "The indexes exist on every install — they back subscription queries that re-run on every write to their table.",
      "actual": "A new DB takes the `schema_needed` path -> _setUpWithSchema, which emits only per-column `isIndexed` DDL. The composite indexes are created only by the v3 MIGRATION, so only UPGRADED installs have them — the inverse of what is wanted.",
      "user_impact": "Slower list queries on exactly the devices the §M0 budgets target, and a performance profile that differs between a fresh install and an upgrade.",
      "suspected_cause": "Index creation placed in a migration rather than in the schema definition.",
      "rootCause": "Confirmed by code reading (WatermelonDB adapters/sqlite/index.js:143-144,199; encodeSchema/index.js:22).",
      "testIds": ["schema.test.ts: VC-024"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": [
        "apps/mobile/src/infra/db/migrations.ts:46-77",
        "apps/mobile/src/infra/db/__tests__/migrations.test.ts:43-53"
      ],
      "regression": false,
      "notes": "FIX: added an unsafeSql hook to schema.ts (appSchema) — WatermelonDB's own supported extension point, confirmed by reading adapters/sqlite/encodeSchema/index.js (encodeSchema calls unsafeSql(sql, \"setup\") for a fresh install) — that appends the same five composite CREATE INDEX statements the v3 migration already applies on an upgrade. Extracted the five statements into a new shared compositeIndexes.ts so migrations.ts and schema.ts can never drift apart, and migrations.ts now maps over that same array instead of repeating the SQL inline. Verified: full jest suite 481/481 pass (3 new tests in schema.test.ts assert the hook exists, appends every composite index on kind:\"setup\", and leaves other DDL kinds untouched), tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-24",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-025",
      "title": "reassertReceipts() on every connect emits an unbounded burst the gateway silently drops",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Receipts / Reconnect",
      "component": "apps/mobile/src/infra/db/receiptStore.ts:141-153 -> domain/sync/SyncEngine.ts:474,890-911",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Every connect with more than ~40 owed conversations",
      "reproducibility": "Deterministic above the inbound budget",
      "preconditions": "Many conversations owing receipts (e.g. after an overnight background).",
      "steps": [
        "Accumulate owed receipts across 50+ conversations.",
        "Reconnect.",
        "Count the receipt frames sent in the first tick and what the server accepted."
      ],
      "expected": "Receipts are chunked within the server's inbound budget, and a dropped frame is retried.",
      "actual": "All `rcpt.sent.*` markers are deleted and every owed conversation is flushed in one unchunked tick. The gateway's DEFAULT_INBOUND_PER_SECOND is 40 (ws-fabric.ts:57) and over-budget frames are DROPPED with only a warn log (ws-fabric.ts:165-168). noteSent then records them as delivered because socket.send() only checks readyState. This self-inflicts the exact backpressure receiptLedger.ts:7-9 was written to avoid.",
      "user_impact": "After a long background, the peer's ticks stay grey for most conversations, and the client believes it has already sent those receipts.",
      "suspected_cause": "No chunking and no send-confirmation on the reassert path.",
      "rootCause": "Confirmed by reading both sides.",
      "testIds": [
        "failureScenarios.test.ts: receipt reassertion after a reconnect with many owed conversations"
      ],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": [
        "apps/mobile/src/infra/db/receiptStore.ts:141-153",
        "D:\\Velchat\\libs\\feature-realtime\\src\\fabric\\ws-fabric.ts:57,165-168"
      ],
      "regression": false,
      "notes": "FIX: flushReceipts() (SyncEngine.ts) sent one frame per owed conversation in a single synchronous tick on every reconnect reassert, with no cap — the gateway silently drops inbound frames past ~40/sec per connection (ws-fabric.ts), and the client never learns which ones were dropped since socket.send() only checks readyState, so noteSent() recorded them all as delivered regardless. Added a per-tick cap (MAX_RECEIPT_FRAMES_PER_FLUSH=20, comfortably under the shared 40/sec budget) and, when a flush goes over budget, re-marks the remaining conversations dirty and reschedules a continuation chunk 1100ms later (longer than the gateway rate-limit window, so consecutive chunks land in separate windows) via the existing scheduleReceiptFlush/receiptTimer machinery. Nothing is ever dropped, only spread across more ticks. Verified: full jest suite 485/485 pass, tsc --noEmit clean, eslint 0 errors. Added 2 new tests (failureScenarios.test.ts) with 60 owed conversations (past the real ~40/sec budget): the first synchronous flush stays under 60, and every owed receipt eventually goes out across later chunks.",
      "jira": "VC-25",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-026",
      "title": "Unread count climbs and the read watermark stalls for messages backfilled into the chat you are reading",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Messaging / Unread",
      "component": "apps/mobile/src/infra/db/messages.ts:301 + domain/sync/SyncEngine.ts:786-794",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Every reconnect while a chat is open",
      "reproducibility": "Deterministic",
      "preconditions": "Sitting in an open chat when a reconnect triggers a backfill.",
      "steps": [
        "Open a chat.",
        "Force a reconnect so pageBackfill runs for that conversation.",
        "Observe the unread badge and the peer's ticks."
      ],
      "expected": "Messages backfilled into the conversation currently on screen are marked read, and the badge stays clear.",
      "actual": "messages.ts:301 bumps unread_count on insert, but pageBackfill calls only noteDelivered — never noteRead or clearUnread. markConversationRead fires only on mount or window growth (useMessages.ts:70, deps [conversationId, meId, limit]).",
      "user_impact": "The unread badge climbs on the chat the user is actively reading, and the sender's ticks stay double-grey.",
      "suspected_cause": "The backfill path does not replay the read side of the on-screen invariant.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-207"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": [
        "apps/mobile/src/infra/db/messages.ts:301",
        "apps/mobile/src/domain/sync/SyncEngine.ts:786-794"
      ],
      "regression": false,
      "notes": "VC-001 makes backfills constant, so this fires often. FIX: pageBackfill now mirrors the live-message path: if the backfilled page lands in the conversation on screen, it marks read (noteRead + clearUnread) instead of only noteDelivered. Verified: full jest suite 454/454 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-26",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-027",
      "title": "'Load older' dead-ends permanently on a deletion hole wider than one page",
      "severity": "P2",
      "priority": "P2",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Pagination / Chat history",
      "component": "apps/mobile/src/domain/sync/SyncEngine.ts:1251-1263",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever a deleted run exceeds the page size",
      "reproducibility": "Deterministic",
      "preconditions": "A conversation with a contiguous run of deleted messages wider than the page limit.",
      "steps": [
        "Delete more than one page worth of consecutive messages.",
        "Scroll up in that conversation and keep requesting older messages."
      ],
      "expected": "Pagination steps past the hole and keeps loading.",
      "actual": "`fresh.length === 0 -> return false`, which the UI reads as end-of-history (useMessages.ts:55). The backend's history filters deleted:false (chat.repository.ts:130), so permanent seq holes are normal — that is gapDetection.ts's whole premise.",
      "user_impact": "Older history becomes permanently unreachable in the UI even though it exists on the server.",
      "suspected_cause": "An empty page is treated as end-of-history rather than as a hole to step over.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["failureScenarios.test.ts: loading older history across a wide deletion hole"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": ["apps/mobile/src/domain/sync/SyncEngine.ts:1251-1263"],
      "regression": false,
      "notes": "FIX: fetchOlderMessages() (SyncEngine.ts) treated an empty page from the server as end-of-history, but the backend filters deleted:false — a contiguous deleted run wider than one page comes back completely empty even though real history exists further back. Since the UI (useMessages.ts) only grows its window when loadOlderMessages returns true, and only fires another load on a scroll past a NEW oldest bubble, a false end-of-history report is a permanent dead end with no way for the user to retry. Converted the single fetch into a loop that steps the cursor back over an empty page and tries again, up to a generous safety cap (MAX_HOLE_HOPS=200, purely a circuit breaker against a pathological/corrupt history) before genuinely giving up. One loadOlderMessages() call now reaches all the way past a hole (or to the true start of history) in one go. Verified: full jest suite 485/485 pass, tsc --noEmit clean, eslint 0 errors. Added 2 new tests (failureScenarios.test.ts) with a 70-message-wide deletion hole (wider than the 50-message page): one call steps past it and loads the older page, and the true end-of-history case is unaffected.",
      "jira": "VC-27",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-028",
      "title": "No event_id dedup anywhere — the documented at-least-once guard does not exist",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Realtime / Idempotency",
      "component": "apps/mobile/src/infra/realtime/socket.ts:7 (doc comment only)",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever the server redelivers an event",
      "reproducibility": "Deterministic given a redelivery",
      "preconditions": "A redelivered durable event (the bus is at-least-once).",
      "steps": [
        "Search the client for an event_id LRU or dedup set.",
        "Redeliver the same message frame twice."
      ],
      "expected": "An event_id LRU rejects the duplicate, as the doc comment at socket.ts:7 implies.",
      "actual": "No dedup structure exists anywhere in the client. Dedup relies entirely on the DB's client_msg_id/seq paths, which VC-020 shows are not race-safe.",
      "user_impact": "Duplicate bubbles on redelivery.",
      "suspected_cause": "Documented but never implemented.",
      "rootCause": "Confirmed by exhaustive search.",
      "testIds": ["VC-UNIT-209"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": ["apps/mobile/src/infra/realtime/socket.ts:7"],
      "regression": false,
      "notes": "",
      "jira": "VC-28"
    },
    {
      "id": "VC-029",
      "title": "Outbox backoff exhaustion is unreachable — MAX_SEND_ATTEMPTS can never fire",
      "severity": "P2",
      "priority": "P2",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Messaging / Outbox",
      "component": "apps/mobile/src/infra/db/outbox.ts:281-286 + domain/sync/SyncEngine.ts:1156",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic",
      "preconditions": "A message that keeps failing.",
      "steps": [
        "Force repeated send failures.",
        "Check whether attempt-count exhaustion is ever consulted."
      ],
      "expected": "After MAX_SEND_ATTEMPTS the message is marked failed by exhaustion.",
      "actual": "outbox.ts:281-286 is only consulted when `permanent === undefined`, and SyncEngine.ts:1156 always passes a boolean — so the branch is dead. syncLogic.test.ts:96-113 tests dead code.",
      "user_impact": "Retry behaviour differs from the documented policy; a message can retry indefinitely on a classification the policy treats as retryable.",
      "suspected_cause": "A call-site refactor left the exhaustion branch unreachable.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-210"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": [
        "apps/mobile/src/infra/db/outbox.ts:281-286",
        "apps/mobile/src/infra/db/__tests__/syncLogic.test.ts:96-113"
      ],
      "regression": false,
      "notes": "FIX: removed the dead attempt-count exhaustion path (MAX_SEND_ATTEMPTS/nextOutboxRetry in syncLogic.ts) that markFailed() fell back to only when permanent was omitted — a fallback no real call site ever triggered (SyncEngine.ts always passes a classified boolean from classifySendFailure()). This mechanism contradicted the documented WhatsApp-style policy (sendFailurePolicy.ts: reachability failures retry forever, never auto-fail by attempt count) and had no basis in the HLD/LLD spec (§L6 outbox semantics: classification-based retryable-vs-permanent only, no attempt cap). Made markFailed()'s permanent parameter required (compiler now prevents the dead path from ever coming back), updated the one test that accidentally relied on the old default (realtime-hardening.test.ts) to pass permanent explicitly, removed the 4 tests for the deleted dead function, and added a new regression test proving a retryable cause stays queued past a high attempt count. Verified: full jest suite 452/452 pass (455 - 4 removed + 1 added), tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-29",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-030",
      "title": "Message window sorts by created_at, not seq — a device clock ahead of the server permanently mis-orders own messages",
      "severity": "P1",
      "priority": "P1",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Messaging / Ordering",
      "component": "apps/mobile/src/infra/db (message window query)",
      "environment": "any",
      "device": "any Android with a skewed clock",
      "os": "any",
      "frequency": "Whenever the device clock leads the server",
      "reproducibility": "Deterministic given the skew",
      "preconditions": "Device clock set ahead of the server.",
      "steps": [
        "Set the device clock 10 minutes ahead.",
        "Send messages and receive replies.",
        "Observe the order in the chat."
      ],
      "expected": "Ordering is by seq. The backend is explicit: 'sort by seq, never timestamp' (backend-integration-reference.md §5).",
      "actual": "The window query sorts by created_at, so a locally stamped own message sorts after later server messages and stays there.",
      "user_impact": "Messages appear in the wrong order and never correct themselves.",
      "suspected_cause": "The window query orders by the local timestamp rather than the server seq.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["messageOrdering.test.ts: VC-030 — a device clock running ahead of the server"],
      "evidence": ["QA/_audit/sync-db-audit.md"],
      "refs": ["docs/backend-integration-reference.md §5"],
      "regression": false,
      "notes": "FIX: two causes, both in the created_at-as-sort-key design. (1) markMessageSent() re-stamped createdAt to the server clock FORWARD ONLY (if (ack.serverTs > m.createdAt)), a rule meant to stop a slow server clock re-burying an offline-composed bubble. Forward-only mixes two clocks in one sort key: on a device whose clock leads the server, an own message keeps its future local stamp and beats every server timestamp that follows it, permanently. The server clock is the same authority that assigns seq, so its timestamp is now adopted in EITHER direction. (2) messageWindowClauses() (extracted from observeMessages so the real query is testable rather than duplicated in a test) now sorts by created_at DESC then seq DESC — seq is the ordering identity per backend-integration-reference §5, and the gateway fans bursts out inside one millisecond, so identical created_at values are normal and were previously resolved by whatever order rows happened to be written. seq is the SECOND key, not the first, because an unsent message has no seq and sorting on it first would file every pending bubble under a null and drag it out of the newest-first window. The pre-existing test never drags a message backwards in time encoded the defect; it was rewritten to assert the concern it actually cared about — that a backwards re-stamp does not change the bubble POSITION — which now holds because seq decides the order. Verified: full jest suite 492/492 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-30",
      "fixedIn": "dev branch, 2026-09-13"
    },
    {
      "id": "VC-031",
      "title": "runQueuedPushActions runs queued notification actions concurrently while claiming to serialize them",
      "severity": "P2",
      "priority": "P2",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Push / Notification actions",
      "component": "apps/mobile/src/features/notifications/model/pushRuntime.ts:301-305",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever two or more actions are queued",
      "reproducibility": "Deterministic",
      "preconditions": "Two replies queued from notifications while the app was killed.",
      "steps": [
        "Queue two notification replies.",
        "Foreground the app so the queue drains.",
        "Observe the order of the resulting sends."
      ],
      "expected": "Sequential, each awaited — as the comment directly above the code states.",
      "actual": "`events.map(e => () => handlePushEvent(e)).map(run => run())` invokes every handler before allSettled, so all run concurrently. Two inline replies write two outbox rows and run markConversationRead concurrently inside a 30s wake window, defeating the ordering collapsePendingEvents works to preserve.",
      "user_impact": "Replies sent from notifications can arrive out of order.",
      "suspected_cause": "The double `.map` invokes eagerly; the intent was a sequential reduce.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-212"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d9"],
      "refs": [
        "apps/mobile/src/features/notifications/model/pushRuntime.ts:301-305",
        "apps/mobile/src/features/notifications/__tests__/pushRuntime.test.ts:215-226"
      ],
      "regression": false,
      "notes": "The existing test passes only because sendText is invoked synchronously at the top of each handler — it cannot fail for the reason it claims.",
      "jira": "VC-31"
    },
    {
      "id": "VC-032",
      "title": "No duplicate-notification suppression, though the monotonic watermark to do it already exists",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Push / Notifications",
      "component": "android/app/src/main/java/com/velchat/push/PushNotifications.kt:200-240",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever FCM redelivers (at-least-once)",
      "reproducibility": "Deterministic given a redelivery",
      "preconditions": "A push redelivered by FCM.",
      "steps": [
        "Receive the same push twice.",
        "Observe the notification thread and the unread count."
      ],
      "expected": "The second delivery is suppressed by the seq watermark PushStore already maintains (PushStore.kt:172-182).",
      "actual": "showMessage calls bumpCount() and appendLine() BEFORE consulting the watermark; setLastSeq is write-only here. The thread line and the count are both doubled.",
      "user_impact": "Duplicate notification lines and an inflated unread badge.",
      "suspected_cause": "The guard was never added; the watermark is maintained but not read on this path.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-213"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d7"],
      "refs": [
        "android/app/src/main/java/com/velchat/push/PushNotifications.kt:200-240",
        "android/app/src/main/java/com/velchat/push/PushStore.kt:172-182"
      ],
      "regression": false,
      "notes": "The fix is one line: `if (seq > 0 && seq <= store.lastSeq(conversationId)) return false`.",
      "jira": "VC-32"
    },
    {
      "id": "VC-033",
      "title": "Native pending-event queue is a lock-free read-modify-write that can wipe a reply enqueued mid-drain",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Push / Notification actions",
      "component": "android/app/src/main/java/com/velchat/push/PushStore.kt:394-407",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever an enqueue overlaps a drain",
      "reproducibility": "Deterministic given the interleaving",
      "preconditions": "A push arriving on the FCM thread while JS drains the queue on the native-modules thread.",
      "steps": [
        "Start a drain.",
        "Have a push enqueue an event during the drain.",
        "Check whether the new event survives."
      ],
      "expected": "Each queued entry is handed to exactly one caller and nothing is lost — which is what the JS side asserts at pushService.ts:190-191 and relies on to skip single-flighting.",
      "actual": "enqueueEvent and takeEvents are unsynchronized, and takeEvents does `prefs.edit().remove(KEY_PENDING)` — deleting the WHOLE key rather than the drained prefix. An event enqueued mid-drain is wiped.",
      "user_impact": "A reply typed from a notification can silently vanish.",
      "suspected_cause": "No synchronization and a whole-key delete instead of a prefix removal.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-214"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d8"],
      "refs": [
        "android/app/src/main/java/com/velchat/push/PushStore.kt:394-407",
        "apps/mobile/src/infra/push/pushService.ts:190-191"
      ],
      "regression": false,
      "notes": "No JVM test source set exists — android/app/src contains only `main`. PushStore is plain-JVM-testable.",
      "jira": "VC-33"
    },
    {
      "id": "VC-034",
      "title": "Retry-After is honored with an unbounded, uncancellable sleep",
      "severity": "P2",
      "priority": "P2",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Networking",
      "component": "apps/mobile/src/infra/network/client.ts:308-312",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever a 429 carries a large Retry-After",
      "reproducibility": "Deterministic",
      "preconditions": "A response with `Retry-After: 86400`.",
      "steps": ["Return a 429 with Retry-After: 86400.", "Observe the request."],
      "expected": "The wait is clamped to a sane ceiling and is cancellable.",
      "actual": "`await wait(retryAfter * 1000)` with no clamp, no ceiling, no negative guard and no abort check. The axios timeout does not apply because the wait is outside the request. The setTimeout is never cleared, and a request cancelled during the wait still fires afterwards. The 5xx backoff path has the same shape.",
      "user_impact": "A request can park for 24 hours; a timer leaks (MOPS-1 reject criterion: a timer without disposal).",
      "suspected_cause": "Missing clamp and disposal.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-215"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d5"],
      "refs": ["apps/mobile/src/infra/network/client.ts:308-312,321-327"],
      "regression": false,
      "notes": "FIX: Retry-After is now clamped to [0, 10s] (MAX_RETRY_AFTER_MS, client.ts) before the wait, instead of obeying a server-supplied value unbounded. Verified: full jest suite 454/454 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-34",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-035",
      "title": "A misbuilt binary reports env:'dev' while talking to production",
      "severity": "P2",
      "priority": "P2",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Configuration",
      "component": "apps/mobile/src/core/config/env.ts:20-31",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Whenever react-native-config fails to inject",
      "reproducibility": "Deterministic",
      "preconditions": "A build where `Config` is empty (stale/cached .env, misbuilt flavor).",
      "steps": [
        "Build with react-native-config unable to inject.",
        "Read the 'backend base' log line."
      ],
      "expected": "The diagnostic tells the truth, or the app refuses to run.",
      "actual": "`name` falls back to 'dev' while `apiBaseUrl` falls back to the PROD host. client.ts:45 then logs {env:'dev', apiBaseUrl:'…duckdns.org'} — the only diagnostic for this failure mode actively lies. A debuggable APK with usesCleartextTraffic=true then points real OTP requests, phone numbers and tokens at production.",
      "user_impact": "Test traffic and real phone numbers hit production from a debug build, undetectably.",
      "suspected_cause": "Two independent fallbacks that can disagree.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-216"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d11"],
      "refs": [
        "apps/mobile/src/core/config/env.ts:20-31",
        "apps/mobile/src/infra/network/client.ts:45"
      ],
      "regression": false,
      "notes": "The prod URL fallback direction is defensible; make `name` fall back to 'prod' alongside it, or log.error when Config.API_BASE_URL is absent. FIX: Extracted the pure resolveAppEnv(rawName, rawApiBaseUrl, rawWsUrl) so the fallback logic is unit-testable. An unrecognised/missing ENV now falls back to 'prod' alongside the URL fallback (previously 'dev'), so a misbuilt binary can never report a safe-sounding name while actually pointing at production. Verified: full jest suite 455/455 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-35",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-036",
      "title": "Cold start after >15 min idle opens the WebSocket with a certainly-expired token",
      "severity": "P2",
      "priority": "P2",
      "status": "Fixed",
      "layer": "CLIENT",
      "feature": "Authentication / Realtime",
      "component": "apps/mobile/src/features/auth/hooks/useAuth.ts:269-273",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Every cold start after 15+ min idle",
      "reproducibility": "Deterministic",
      "preconditions": "A stored access token older than its 900s TTL.",
      "steps": [
        "Sign in.",
        "Leave the app closed for 20 minutes.",
        "Cold start and watch the WebSocket handshake."
      ],
      "expected": "The token is refreshed before the socket is opened.",
      "actual": "hasSession() is `Boolean(getAccessToken())` — true for an EXPIRED token — so bootstrap hydrates and never refreshes. SyncEngine.ts:449-451 hands the dead token to socket.connect. Recovery exists (4001 -> refresh -> reconnect), so it is not silent, but every such cold start costs a guaranteed failed handshake plus a refresh plus a reconnect.",
      "user_impact": "Slower first sync on every cold start after idle, and avoidable battery/network cost on the reference device.",
      "suspected_cause": "hasSession() checks presence, not validity. accessTokenExpiresInMs() already exists and is used by the push runtime but not here.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-UNIT-217"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#d13"],
      "refs": ["apps/mobile/src/infra/network/tokens.ts:83-85,98"],
      "regression": false,
      "notes": "FIX: hasValidSession() added (tokens.ts); useAuthBootstrap now refreshes before hydrating on an expired-but-present token. hasSession() deliberately left presence-only to protect SyncEngine reconnect guards. Verified: full jest suite 454/454 pass, tsc --noEmit clean, eslint 0 errors.",
      "jira": "VC-36",
      "fixedIn": "dev branch, 2026-09-12"
    },
    {
      "id": "VC-037",
      "title": "The access token travels in the WebSocket query string",
      "severity": "P2",
      "priority": "P2",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Security / Transport",
      "component": "apps/mobile/src/infra/realtime/socket.ts:104-108",
      "environment": "any",
      "device": "any Android",
      "os": "any",
      "frequency": "Always",
      "reproducibility": "Deterministic",
      "preconditions": "Any realtime connection.",
      "steps": ["Open the socket.", "Inspect the connect URL."],
      "expected": "The token rides the `Authorization` header, which the backend already accepts.",
      "actual": "It is appended as `?token=`. Query strings land in every proxy/CDN access log along the path (Caddy on the Azure VM, Render's edge). The stated justification — 'RN's WebSocket can't set headers reliably' — is weaker than it reads: RN's WebSocket constructor accepts a third {headers} argument on both platforms.",
      "user_impact": "Access tokens accumulate in server access logs.",
      "suspected_cause": "A platform limitation that no longer holds.",
      "rootCause": "Confirmed by code reading.",
      "testIds": ["VC-SEC-105"],
      "evidence": ["QA/_audit/auth-network-push-audit.md#s5"],
      "refs": [
        "apps/mobile/src/infra/realtime/socket.ts:104-108",
        "docs/backend-integration-reference.md §4"
      ],
      "regression": false,
      "notes": "The one leak-into-logs path in the client itself is already mitigated by redact.ts's JWT pattern.",
      "jira": "VC-37"
    },
    {
      "id": "VC-038",
      "title": "Jest reports a worker that failed to exit gracefully — a leaked timer or handle in the test path",
      "severity": "P3",
      "priority": "P3",
      "status": "Open",
      "layer": "CLIENT",
      "feature": "Test infrastructure",
      "component": "apps/mobile jest suite",
      "environment": "local",
      "device": "n/a",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "None.",
      "steps": ["Run `pnpm --filter @velchat/mobile test`.", "Read the final lines of output."],
      "expected": "The suite exits cleanly.",
      "actual": "'A worker process has failed to exit gracefully and has been force exited. This is likely caused by tests leaking due to improper teardown.' All 450 tests pass, so this is hygiene rather than a failure — but §M7 requires every long-lived resource to be owned and disposable, and this is the signal that one is not.",
      "user_impact": "None directly. It masks real leak regressions and slows CI.",
      "suspected_cause": "An undisposed timer or listener in a module the suite imports (the push/sync modules arm intervals at import time).",
      "rootCause": "Unknown — requires engineering investigation. Run with --detectOpenHandles to localise.",
      "testIds": ["VC-UNIT-001"],
      "evidence": ["QA/evidence/logs/jest-baseline-2026-09-11.log"],
      "refs": [],
      "regression": false,
      "notes": "",
      "jira": "VC-38"
    },
    {
      "id": "VC-039",
      "title": "Any authenticated user can READ and WRITE any conversation — no authorization on the chat routes, and conversation ids are derivable offline",
      "severity": "P0",
      "priority": "P0",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Security / Messaging",
      "component": "messaging-service /chat/* and identity-service /conversations/*",
      "environment": "local backend axis6, gateway :8080",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "One ordinary VelChat account (any account, no special role). The target conversation id, which is COMPUTED rather than guessed.",
      "steps": [
        "Provision three accounts A, B and C. A and B open a DM; A sends \"TOP SECRET between A and B\".",
        "C is not a member of that conversation and holds only its own ordinary token.",
        "C derives the conversation id offline: \"dm-\" + the first 32 hex chars of sha256(\"<loAccountId>|<hiAccountId>\"). No secret is involved (apps/mobile/src/infra/db/dmId.ts:29-38 is a byte-exact port of the backend).",
        "C calls GET /chat/conversations/<id>/messages?afterSeq=0 with its own token.",
        "C calls GET /conversations/<id> and GET /conversations/<id>/members.",
        "C calls POST /chat/messages for that conversation id with its own token.",
        "B re-reads the conversation."
      ],
      "expected": "Every one of those calls returns 403/404 for a non-member. Authentication is not authorization: membership must gate reads, metadata, member enumeration and writes.",
      "actual": "Measured: (1) C reads the messages — 200, [\"TOP SECRET between A and B\"]. (2) With NO token the same call is correctly 401, so authentication IS enforced. (3) C reads conversation details — 200. (4) C enumerates the member list — 200, both account ids returned. (5) C WRITES into the DM — 201 with seq 2. (6) B then sees [\"TOP SECRET between A and B\",\"C INTRUDED HERE\"]. Authorization is absent entirely on these routes.",
      "user_impact": "Any single registered user can read every private conversation in the system and inject messages that appear to the real members as part of their chat. Because the DM id is sha256 of the sorted account-id pair, no enumeration or guessing is needed — and account ids are themselves obtainable from the member list of any conversation the attacker can already reach, which is all of them. This is a complete breach of message confidentiality and integrity.",
      "suspected_cause": "The chat and conversation controllers derive the principal from the JWT (which is why impersonation via senderId is correctly rejected — see VC-SEC-021) but never check that principal against the conversation membership projection.",
      "rootCause": "Confirmed by live reproduction: authentication is enforced (401 without a token) while authorization is not (200/201 for a non-member holding any valid token).",
      "testIds": ["VC-SEC-020", "VC-2DEV-024"],
      "evidence": [
        "QA/evidence/api/VC-039_FAIL_2026-09-11_idor-read-write.log",
        "QA/evidence/api/VC-039_repro.mjs",
        "QA/evidence/api/api-2026-09-11.jsonl"
      ],
      "refs": [
        "apps/mobile/src/infra/db/dmId.ts:29-38",
        "D:\\Velchat\\libs\\feature-chat\\src\\chat\\chat.controller.ts"
      ],
      "regression": false,
      "notes": "Highest-impact finding of the run. It also leaves E2EE as the only remaining confidentiality control — and E2EE is not implemented yet (the client still sends plaintext `content`), so there is no compensating control today.",
      "jira": "VC-39"
    },
    {
      "id": "VC-040",
      "title": "A push endpoint can be registered against another account's userId, redirecting their push wake-ups",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Security / Push",
      "component": "platform-service /notifications/endpoints",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "Two accounts A and B. B holds its own valid token.",
      "steps": [
        "B POSTs /notifications/endpoints with its own bearer token and its own deviceId, but userId set to A's accountId and a push token B controls.",
        "Observe the response."
      ],
      "expected": "Rejected, or the row is bound to the token's principal (B). The client fills this userId from its own local state (apps/mobile/src/infra/push/api.ts:24-28, pushService.ts:455-460), so the server must never trust it.",
      "actual": "201 Created. The endpoint row is accepted with a userId the caller merely asserted.",
      "user_impact": "B receives A's push wake-ups — conversation ids, message types, and previews for non-E2EE chats. Combined with VC-039 the attacker can then read the referenced messages in full.",
      "suspected_cause": "The endpoint upserts on device_id and trusts the body's userId rather than binding it to the verified token.",
      "rootCause": "Confirmed by live reproduction. This upgrades audit finding S7 from a suspicion to a defect.",
      "testIds": ["VC-SEC-024"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": [
        "apps/mobile/src/infra/push/api.ts:24-28",
        "apps/mobile/src/features/notifications/api/prefs.ts:23-36"
      ],
      "regression": false,
      "notes": "The same body-trusts-userId pattern appears in the notification prefs API and should be audited with it.",
      "jira": "VC-40"
    },
    {
      "id": "VC-041",
      "title": "POST /chat/messages accepts a conversationId that does not exist",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Messaging / Validation",
      "component": "messaging-service /chat/messages",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "Any valid token.",
      "steps": [
        "POST /chat/messages with conversationId \"dm-does-not-exist-0000\" and otherwise valid fields."
      ],
      "expected": "4xx — a message cannot belong to a conversation that does not exist.",
      "actual": "201 Created. The message is persisted against a conversation id with no conversation row and no members.",
      "user_impact": "Unbounded writes into a table with no owner, and it is the same missing check that makes VC-039 exploitable for writes. Messages accumulate that nobody can read or moderate.",
      "suspected_cause": "No existence or membership validation before the insert.",
      "rootCause": "Confirmed by live reproduction. Same missing check as VC-039.",
      "testIds": ["VC-2DEV-024"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": [],
      "regression": false,
      "notes": "Fix together with VC-039.",
      "jira": "VC-41"
    },
    {
      "id": "VC-042",
      "title": "POST /chat/messages accepts an empty or whitespace-only message",
      "severity": "P2",
      "priority": "P2",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Messaging / Validation",
      "component": "messaging-service /chat/messages",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Always",
      "reproducibility": "100%",
      "preconditions": "A conversation the sender belongs to.",
      "steps": [
        "POST /chat/messages with content \"\".",
        "Repeat with \"   \" and with \"\\n\\n\"."
      ],
      "expected": "4xx for each — an empty bubble is not a message.",
      "actual": "201 Created for all three. Empty messages are persisted and fan out to the recipient.",
      "user_impact": "Empty bubbles in the conversation, and a trivial way to bump a chat to the top of the inbox with no content.",
      "suspected_cause": "The content field is validated for type but not for emptiness after trimming.",
      "rootCause": "Confirmed by live reproduction.",
      "testIds": ["VC-2DEV-023"],
      "evidence": ["QA/evidence/api/api-2026-09-11.jsonl"],
      "refs": [],
      "regression": false,
      "notes": "The mobile composer disables send for empty input, so this is only reachable from a non-VelChat client — but it is still an unvalidated write.",
      "jira": "VC-42"
    },
    {
      "id": "VC-043",
      "title": "Concurrent refresh-token requests can all succeed — rotation is not race-safe",
      "severity": "P1",
      "priority": "P1",
      "status": "Open",
      "layer": "BACKEND",
      "feature": "Authentication / Tokens",
      "component": "identity-service /auth/token/refresh",
      "environment": "local backend axis6",
      "device": "node-harness",
      "os": "win32-x64",
      "frequency": "Observed 1/1 (3 of 3 concurrent requests succeeded)",
      "reproducibility": "Needs repeat runs to establish a rate, but reproduced on first attempt",
      "preconditions": "A valid, not-yet-rotated refresh token.",
      "steps": [
        "Provision an identity and note its refresh token R.",
        "Fire three POST /auth/token/refresh requests with R SIMULTANEOUSLY (Promise.all, no sequencing).",
        "Count how many return a 2xx with a new token set."
      ],
      "expected": "At most one request succeeds; rotation must be atomic so the first winner invalidates R for every other in-flight request. Otherwise reuse detection (VC-AUTH-024, confirmed working for a SEQUENTIAL replay) cannot do its job against a concurrent one, and the \"whole family revoked on reuse\" security property has a race window.",
      "actual": "All 3 of 3 concurrent requests returned a new, distinct token set. None were rejected.",
      "user_impact": "A refresh token, once briefly exposed (e.g. captured by a MITM despite TLS, or replayed from a compromised client queue), can be redeemed multiple times in the race window before the first rotation commits — defeating the single-use guarantee rotation is meant to provide.",
      "suspected_cause": "The refresh handler likely reads-then-writes the token record without a compare-and-swap or row lock across concurrent requests for the same token.",
      "rootCause": "Unknown — requires engineering investigation.",
      "testIds": ["VC-AUTH-027"],
      "evidence": ["QA/reports/results.jsonl"],
      "refs": ["docs/backend-integration-reference.md §3"],
      "regression": false,
      "notes": "Found during the 2026-09-11 re-verification pass (run verify-002), not in the original audit.",
      "jira": "VC-43"
    },
    {
      "id": "VC-044",
      "status": "Open",
      "regression": false,
      "evidence": [],
      "refs": [],
      "testIds": [],
      "title": "Contact name/contact update does not sync immediately in chat UI",
      "severity": "P2",
      "priority": "P2",
      "layer": "CLIENT",
      "feature": "Contact Sync / Contact Update",
      "component": "New Chat / Chat Screen / Contacts",
      "environment": "Production",
      "device": "Android",
      "os": "Android",
      "frequency": "Reported",
      "reproducibility": "Unconfirmed — reported from manual production testing",
      "preconditions": "A new contact is saved in the phone's contact list.",
      "steps": [
        "Save a new contact in the phone's address book.",
        "Open VelChat and enter that phone number / find the conversation.",
        "Observe only the phone number is shown, not the saved contact name.",
        "Send a message and exit the chat.",
        "Re-check — the contact name now appears, and multiple pending contact updates appear together instead of as they happen."
      ],
      "expected": "After saving the contact, VelChat should immediately detect/sync the contact and display the saved contact name consistently wherever that contact appears.",
      "actual": "Initially only the phone number is displayed. After opening/chatting and then exiting the chat, the contact name appears. The contact information does not update immediately, and multiple contact-related updates appear together instead of updating immediately.",
      "user_impact": "Users see raw phone numbers instead of contact names until they interact with the chat, which reads as broken contact sync.",
      "suspected_cause": "Not yet investigated.",
      "rootCause": "Not yet investigated — reported from manual production QA testing, needs code-level triage.",
      "notes": "Reported by Tusha Bhadouriya during production QA testing.",
      "assignee": "Tusha Bhadouriya",
      "assigneeAccountId": "712020:f2e5ac48-d89a-4817-850b-66bd48f7dd28",
      "jira": "VC-44"
    },
    {
      "id": "VC-045",
      "status": "Open",
      "regression": false,
      "evidence": [],
      "refs": [],
      "testIds": [],
      "title": "Real-time messaging and notification-reply sync needs multi-scenario two-device verification",
      "severity": "P1",
      "priority": "P1",
      "layer": "CLIENT",
      "feature": "Real-Time Messaging / Notifications",
      "component": "Chat Screen / Notification",
      "environment": "Production",
      "device": "Android",
      "os": "Android",
      "frequency": "N/A — verification task",
      "reproducibility": "N/A",
      "preconditions": "Two physical devices, each signed into a separate VelChat account.",
      "steps": [
        "Exchange messages continuously between two devices for 10-13 minutes across different messaging scenarios via the normal Chat Page.",
        "Test notification-based replies from both devices for 6-8 minutes.",
        "Remove both apps from the background (swipe away / kill) and reply ONLY through notifications.",
        "Test a mixed scenario: one device replies through the notification while the other sends/replies normally from the Chat Page."
      ],
      "expected": "Messages and replies are delivered and synchronized correctly in real time across both devices in every tested scenario, including normal Chat Page messaging, notification replies, background-removed app state, and mixed notification + Chat Page messaging.",
      "actual": "Not yet executed — result pending. Logged as a required verification pass across the scenarios above.",
      "user_impact": "Any gap here directly breaks core messaging reliability and notification-reply delivery, the two most-used flows in the app.",
      "suspected_cause": "N/A — this is a test-execution task, not yet a diagnosed defect.",
      "rootCause": "N/A — pending test execution.",
      "notes": "This entry tracks a two-device manual verification pass, not a confirmed defect. Assigned to Tusha Bhadouriya to execute and report actual pass/fail per scenario back into this bug.",
      "assignee": "Tusha Bhadouriya",
      "assigneeAccountId": "712020:f2e5ac48-d89a-4817-850b-66bd48f7dd28",
      "jira": "VC-45"
    },
    {
      "id": "VC-046",
      "status": "Open",
      "regression": false,
      "evidence": [],
      "refs": [],
      "testIds": [],
      "title": "Online/offline presence status updates with a noticeable delay",
      "severity": "P2",
      "priority": "P2",
      "layer": "CLIENT",
      "feature": "Online/Offline Status",
      "component": "Chat Screen / Contacts",
      "environment": "Production",
      "device": "Android",
      "os": "Android",
      "frequency": "Reported",
      "reproducibility": "Unconfirmed — reported from manual production testing",
      "preconditions": "Two users in an active/visible chat.",
      "steps": [
        "Have the peer come online or go offline.",
        "Observe how long the status indicator takes to reflect the change."
      ],
      "expected": "When a user comes online or goes offline, their status should update and display immediately to other users.",
      "actual": "The user's online/offline status is displayed after a noticeable delay.",
      "user_impact": "Users see stale presence information, which can mislead them about whether the other person is actually available.",
      "suspected_cause": "Not yet investigated — could be a client-side polling/debounce delay or a backend presence-propagation delay.",
      "rootCause": "Not yet investigated.",
      "notes": "Reported by Tusha Bhadouriya during production QA testing. Root cause (client vs backend) not yet isolated.",
      "assignee": "Tusha Bhadouriya",
      "assigneeAccountId": "712020:f2e5ac48-d89a-4817-850b-66bd48f7dd28",
      "jira": "VC-46"
    },
    {
      "id": "VC-047",
      "status": "Open",
      "regression": false,
      "evidence": [],
      "refs": [],
      "testIds": [],
      "title": "Chat List shows the registered VelChat name instead of the saved contact name until a chat is opened",
      "severity": "P2",
      "priority": "P2",
      "layer": "CLIENT",
      "feature": "Contact Name Sync",
      "component": "Chat List / Message Yourself / Contact List",
      "environment": "Production",
      "device": "Android",
      "os": "Android",
      "frequency": "Reported",
      "reproducibility": "Reported reproducible via Message Yourself",
      "preconditions": "A contact is saved locally under a custom name different from their VelChat registered name (e.g. saved as \"Aayush Sir\" for an account registered as \"Aayush Jain\").",
      "steps": [
        "Save a contact's number under a custom local name that differs from their VelChat display name.",
        "Open the Chat List — observe the registered VelChat name is shown instead of the saved contact name.",
        "Open the contact's chat via Message Yourself and return to the Chat List.",
        "Observe the name has now updated to the saved contact name."
      ],
      "expected": "The saved contact name should be displayed immediately in the Chat List wherever the contact is shown, without requiring the user to open the chat first.",
      "actual": "Initially the registered VelChat name (\"Aayush Jain\") is displayed. After opening the contact's chat through Message Yourself and returning to the Chat List, it changes to the saved contact name (\"Aayush Sir\").",
      "user_impact": "Users see the wrong display name for their own saved contacts until they happen to open that specific chat, which is confusing and looks like a sync bug.",
      "suspected_cause": "Possibly the same underlying cause as the contact-sync bug reported separately — the Chat List may resolve the display name before local contact-name resolution/caching has run.",
      "rootCause": "Not yet investigated.",
      "notes": "Reported by Tusha Bhadouriya during production QA testing. May share a root cause with the contact-sync issue reported as a separate entry.",
      "assignee": "Tusha Bhadouriya",
      "assigneeAccountId": "712020:f2e5ac48-d89a-4817-850b-66bd48f7dd28",
      "jira": "VC-47"
    },
    {
      "id": "VC-048",
      "status": "Open",
      "regression": false,
      "evidence": [],
      "refs": [],
      "testIds": [],
      "title": "Message notification omits the sender's name and tapping it does not open the chat",
      "severity": "P2",
      "priority": "P2",
      "layer": "CLIENT",
      "feature": "Push Notifications / Notification Navigation",
      "component": "Notification / Chat Screen",
      "environment": "Production",
      "device": "Android",
      "os": "Android",
      "frequency": "Reported",
      "reproducibility": "Unconfirmed — reported from manual production testing",
      "preconditions": "A message is received while the app is backgrounded.",
      "steps": [
        "Receive a new message while VelChat is backgrounded.",
        "Observe the notification only shows a generic count (e.g. \"3 messages\"), not who sent them.",
        "Tap the notification.",
        "Observe VelChat does not open directly — the user has to manually find and open the app from the app list."
      ],
      "expected": "The notification should display the sender's name along with the message/number of messages. Tapping the notification should directly open VelChat and navigate to the relevant chat.",
      "actual": "The notification only shows VelChat and the number of messages, without identifying the sender. Tapping the notification does not open the app, requiring the user to manually open VelChat.",
      "user_impact": "Users cannot tell who messaged them from the notification, and tapping it fails to take them to the conversation — a core notification-usability failure.",
      "suspected_cause": "Not yet investigated — possibly related to the existing duplicate-notification/pending-event defects already tracked in this registry, but not confirmed the same root cause.",
      "rootCause": "Not yet investigated.",
      "notes": "Reported by Tusha Bhadouriya during production QA testing.",
      "assignee": "Tusha Bhadouriya",
      "assigneeAccountId": "712020:f2e5ac48-d89a-4817-850b-66bd48f7dd28",
      "jira": "VC-48"
    },
    {
      "id": "VC-049",
      "status": "Fixed",
      "regression": false,
      "evidence": [],
      "refs": [],
      "testIds": ["profileGate.test.ts: VC-049"],
      "title": "Email-prompt bottom sheet reappears on every login even when an email is already saved",
      "severity": "P1",
      "priority": "P1",
      "layer": "CLIENT",
      "feature": "Authentication / Profile / Email Verification",
      "component": "Home Screen",
      "environment": "Production",
      "device": "Android",
      "os": "Android",
      "frequency": "Every login",
      "reproducibility": "Deterministic per report — every login",
      "preconditions": "An account that already has an email address linked/saved.",
      "steps": [
        "Log in to an account that already has an email address associated with it.",
        "Observe the email-prompt bottom sheet on the Home Screen."
      ],
      "expected": "After logging in, the bottom sheet asking for an email should ONLY appear if the user has not added/linked an email. If the email is already present, the bottom sheet should not appear.",
      "actual": "The bottom sheet asking for email opens repeatedly after every login, regardless of whether the user already has an email saved.",
      "user_impact": "Users with an email already on file are repeatedly nagged for it on every login, a persistent, avoidable annoyance that looks like the app isn't remembering their profile.",
      "suspected_cause": "The bottom-sheet trigger likely checks a stale/cached email-presence flag instead of current profile state, or the check itself is missing.",
      "rootCause": "Not yet investigated.",
      "notes": "FIX (root-caused): the email is NOT a directory-profile field — profileShape.ts states it is a separately-verified identifier owned by the auth-service (GET /auth/account). useProfileGate decided purely from the LOCAL MMKV mirror, and purgeAccountData() wipes KVKeys.email on every sign-out, so immediately after any login the mirror is empty and the gate prompted unconditionally. useAccountInfo() does fetch the real email and mirror it, but it runs in parallel from AppTabs, so its answer always lost the race — and dismissal is session-only, which is why it returned every single login. The gate now asks the auth-service itself before deciding, via a new pure shouldPromptForProfile() (features/user/model/profileGate.ts): a mirrored email means never prompt; a server email means never prompt (and is re-mirrored so later launches short-circuit with no network); an UNANSWERED server (in flight or unreachable) means WAIT rather than prompt, which also removes the flash of the sheet before the answer lands. Only a definitive server no-email prompts. Verified: full jest suite 492/492 pass (5 new decision tests), tsc --noEmit clean, eslint 0 errors, no eslint-plugin-boundaries violation for the user→auth import (authApi depends only on infra, so no cycle). Not reproduced on a device — no Android hardware attached to this machine; fix is verified at the unit level against the exact reported condition.",
      "assignee": "Tusha Bhadouriya",
      "assigneeAccountId": "712020:f2e5ac48-d89a-4817-850b-66bd48f7dd28",
      "jira": "VC-49",
      "fixedIn": "dev branch, 2026-09-13"
    }
  ]
}
