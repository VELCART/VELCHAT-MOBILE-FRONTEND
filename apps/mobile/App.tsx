/**
 * Entry re-export. The real root (providers + navigation) lives in
 * src/app/App.tsx per §L2 / §M4; this keeps index.js's import path stable.
 *
 * @format
 */
export { default } from './src/app/App';
