# VelChat prod flavor — PUBLIC build config only (never secrets).
# NOTE: placeholder host — confirm the production ingress strategy (per-service
# Render hosts vs a gateway) before release. See docs/backend-integration-reference.md §8.
ENV=prod
API_BASE_URL=https://gateway.velchat.app
WS_URL=wss://gateway.velchat.app/ws
