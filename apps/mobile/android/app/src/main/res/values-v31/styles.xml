<resources>

    <!-- Android 12+ splash screen: white field + the VelChat logo (launcher
         foreground), so app-open matches the white onboarding. Mirrors the base
         AppTheme (values/styles.xml) and adds the splash attributes. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <!-- System navigation bar = the app's bottom tab surface (light). -->
        <item name="android:navigationBarColor">#FFFFFF</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

</resources>
