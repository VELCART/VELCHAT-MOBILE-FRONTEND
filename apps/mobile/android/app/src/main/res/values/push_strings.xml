<!--
  Notification strings + the accent colour, kept in their own file so the push layer does not
  reach into `strings.xml`/`styles.xml` (which the RN template owns and regenerates).

  `translatable="false"` is NOT set: these are the only user-visible strings the native side
  produces, and they are the first thing a localisation pass would want.
-->
<resources>
    <!-- Notification channels (user-visible in Android Settings → Notifications). -->
    <string name="push_channel_messages_name">Messages</string>
    <string name="push_channel_messages_desc">New messages in your chats</string>
    <string name="push_channel_calls_name">Calls</string>
    <string name="push_channel_calls_desc">Incoming voice and video calls</string>

    <!-- The brief foreground service that finishes a reply typed into a notification. Android
         requires such a service to be visible; the channel is MIN importance because the user
         just pressed send and does not need telling. -->
    <string name="push_channel_working_name">Sending</string>
    <string name="push_channel_working_desc">Shown briefly while a reply you sent from a notification finishes sending</string>
    <string name="push_working_title">Sending your reply…</string>

    <!-- Notification body. The push carries the message text only when the server holds readable
         plaintext (see the backend's preview.ts); these are the fallbacks for everything else. -->
    <string name="push_new_message">New message</string>
    <plurals name="push_new_messages">
        <item quantity="one">%d new message</item>
        <item quantity="other">%d new messages</item>
    </plurals>
    <string name="push_incoming_call">Incoming call</string>

    <!-- How the user is named in their own notification thread (NotificationCompat.MessagingStyle
         requires a Person for the local user). -->
    <string name="push_you">You</string>

    <!-- Attachments have no text body. The push carries the TYPE so these labels can be in the
         user's language rather than the server's. -->
    <string name="push_kind_image">Photo</string>
    <string name="push_kind_video">Video</string>
    <string name="push_kind_audio">Voice message</string>
    <string name="push_kind_file">Document</string>
    <string name="push_kind_location">Location</string>
    <string name="push_kind_contact">Contact</string>
    <string name="push_kind_poll">Poll</string>

    <!-- Actions. -->
    <string name="push_action_reply">Reply</string>
    <string name="push_action_reply_hint">Message</string>
    <string name="push_action_mark_read">Mark as read</string>
    <string name="push_action_mute">Mute</string>
    <string name="push_reply_sending">Sending…</string>

    <!-- Group summary. -->
    <string name="push_summary_title">VelChat</string>
    <plurals name="push_summary_text">
        <item quantity="one">%d new message</item>
        <item quantity="other">%d new messages</item>
    </plurals>

    <!-- The app's brand accent. The shipped theme is monochrome (design-system/tokens.ts:
         brandFrom #0B0B0C light / #F5F5F7 dark), so the notification tint is that same near-black.
         It used to be the violet #7C5CFF from the abandoned violet->pink direction, a hue that
         appears nowhere in the running app (VC-055). Android lightens this itself where a dark
         accent would be unreadable against the shade. -->
    <color name="push_accent">#0B0B0C</color>
</resources>
